# Core Django
Django>=4.2,<5.0

# Django REST Framework
djangorestframework
djangorestframework-simplejwt

# Database (MySQL)
pymysql

# Redis & Caching
redis
django-redis

# Celery (Async Tasks)
celery

# File Upload / Images
Pillow

# Environment variables
python-dotenv

# CORS (if frontend is React)
django-cors-headers

# Filtering & search support
django-filter

# Production helpers (optional but safe)
gunicorn

# Logging & utilities
pytz

# daphne
daphne -b 127.0.0.1 -p 8000 backend.asgi:application
